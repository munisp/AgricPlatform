# Stage 22 (PR #37): false positive — test idempotency key literal in an
# intermediate branch commit. Not a credential; entropy just over the
# generic-api-key threshold. Squash-merge erases the branch history, so this
# fingerprint never exists on main. Cf. .trivyignore VEX discipline.
b962cb7bdcbedb6876d45d1f78cd90c1875c774d:apps/api/src/modules/agent-banking/agent-banking.service.spec.ts:generic-api-key:261

# Stage 27 smoke-job repair (commit 23fcc5c3): false positive on a documented
# CI-only throwaway value in the smoke job's break-glass env block. All
# "ci-smoke-*" values are placeholders that exist nowhere outside CI logs;
# real deployments provision high-entropy secrets via the secret store, and
# the API boot itself rejects these values as too weak were they ever reused
# (assertProductionSecretStrength). Same class as the PR #37 entry above.
23fcc5c3941adc5653a3ad60ca82d212f2c7ad84:.github/workflows/ci.yml:generic-api-key:279
