/** Mock of expo-constants: the test API base URL, no native modules. */
const Constants = {
  expoConfig: {
    extra: {
      apiBaseUrl: 'https://api.test/api/v1'
    }
  }
};

export default Constants;
