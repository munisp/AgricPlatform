import type { DeepPartial } from '../types';
import type { Dictionary } from './en';

/**
 * Igbo (ig) — EMPTY SCAFFOLD.
 * Keys translate incrementally; anything missing falls back to English at
 * runtime. No machine-translated content is committed — translations need
 * native-speaker review first (see docs/roadmap/observability-a11y-plan.md).
 */
export const ig: DeepPartial<Dictionary> = {};
