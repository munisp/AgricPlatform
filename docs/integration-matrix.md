# Integration Matrix — AgricPlatform

All external systems are integrated as **provider adapters behind interfaces** (SPEC contract 4): a local stub keeps CI deterministic and the repository buildable without secrets; a production driver is selected via environment flags. The current reference implementation centralises provider stubs and status reporting in `apps/api/src/modules/integrations/` (notably `adapters.ts`, `integrations.service.ts`, and `integrations.controller.ts`); infrastructure-level components live under `infra/`. Provider-specific production adapters may later be split into `apps/api/src/integrations/<provider>/` without changing the contract. This matrix is the single reference for what each integration does, how it behaves locally, what credentials production needs, and how it is verified.

**Verification classes** (from `docs/requirements-traceability.md` §6):
- **Code** — verifiable in CI with stubs, no credentials.
- **Sandbox** — verifiable with test credentials / self-hosted instance; proves the adapter contract only.
- **Live** — requires production credentials and/or a signed agreement; **cannot be live-verified without them**. These remain External dependency items on the launch-blocker list.

Driver flags defined at baseline in `.env.example`: `SMS_DRIVER`, `WHATSAPP_DRIVER`, `EMAIL_DRIVER`, `PUSH_DRIVER`, `PAYMENT_DRIVER`, `SEARCH_DRIVER`, `WEATHER_DRIVER`, `LMS_DRIVER`, `COMMUNITY_DRIVER` (each `stub | sandbox | production`). Integrations without a driver flag yet are noted as **flag TBD** and default to stub/no-op.

## Wave P1 live driver implementations

Real HTTP drivers now sit behind the adapter port in `apps/api/src/modules/integrations/drivers/`. Every call uses the global fetch with a 5-second `AbortController` timeout and no in-adapter retries (callers own retry policy); errors map to `ProviderConfigError` (fail closed), `ProviderHttpError` (4xx/5xx) or `ProviderRequestError` (timeout/network). The stub stays the default driver everywhere; in production the API refuses to boot when a non-stub driver lacks its credentials (`assertProductionDriverConfig` plus eager driver construction in `IntegrationsService`).

| Driver | Flag | Live behaviour | Credentials (env) | Code |
| --- | --- | --- | --- | --- |
| SMS — Termii + Twilio failover | `SMS_DRIVER` | Termii `/api/sms/send` + `/api/sms/otp/send`; falls back to Twilio `Messages.json` on primary failure (config errors are not retried) | `TERMII_API_KEY` + `TERMII_SENDER_ID`, or `TWILIO_ACCOUNT_SID`/`TWILIO_AUTH_TOKEN`/`TWILIO_FROM_NUMBER` (both = failover) | `drivers/sms.drivers.ts` |
| WhatsApp — 360dialog | `WHATSAPP_DRIVER` | Template send (`/v1/messages`) + free-form text; inbound webhook normalisation to provider-agnostic messages | `WHATSAPP_360DIALOG_API_KEY` (+ optional `WHATSAPP_360DIALOG_NAMESPACE`) | `drivers/whatsapp.drivers.ts` |
| Email — Mailgun / SendGrid | `EMAIL_DRIVER` | Mailgun `v3/{domain}/messages` preferred; SendGrid `v3/mail/send` alternate; `EMAIL_FROM` overrides sender | `MAILGUN_API_KEY` + `MAILGUN_DOMAIN`, or `SENDGRID_API_KEY` | `drivers/email.drivers.ts` |
| Push — OneSignal | `PUSH_DRIVER` | `api/v1/notifications` to external user ids or segments | `ONESIGNAL_APP_ID` + `ONESIGNAL_REST_API_KEY` | `drivers/push.drivers.ts` |
| Payments — Paystack | `PAYMENT_DRIVER` | `transaction/initialize` + `/verify`, `refund`, `transfer` (escrow release); HMAC-SHA512 webhook helper `verifyPaystackSignature` (the shared `/integrations/webhooks/:provider` endpoint keeps HMAC-SHA256 via `WEBHOOK_SIGNING_SECRET`) | `PAYSTACK_SECRET_KEY` | `drivers/payments.drivers.ts` |
| Payments — Flutterwave | `PAYMENT_DRIVER` | `v3/payments`, `transactions/verify_by_reference`, refunds, `v3/transfers`; `verif-hash` webhook helper `verifyFlutterwaveSignature` | `FLUTTERWAVE_SECRET_KEY` (+ `FLUTTERWAVE_VERIF_HASH` for native webhooks) | `drivers/payments.drivers.ts` |
| Search — Meilisearch | `SEARCH_DRIVER` | `indexes/{uid}/search` with type/state filters + ranking scores; document index/remove; `SearchProvider` port mirrors the stub `SearchService` result contract | `MEILISEARCH_HOST` (+ optional `MEILISEARCH_API_KEY`, `MEILISEARCH_INDEX`) | `drivers/search.drivers.ts` |
| Weather — Open-Meteo | `WEATHER_DRIVER` | Keyless real default feed (`api.open-meteo.com/v1/forecast`) with the Nigeria state→lat/lon centroid table and a 15-minute cache (Redis via the shared KeyValueStore when `REDIS_URL` is set); wired into the advisory `weatherFor` snapshot path | none (open API); NiMet stays MoU-gated | `drivers/weather.drivers.ts`, `drivers/nigeria-states.ts` |
| Market data — FEWS NET / NiMet | `MARKET_DATA_DRIVER=live` | Scheduled ingestion (default 6h, `MARKET_DATA_POLL_INTERVAL_MS`) normalising price points into `advisory.commodity_prices` (`infra/postgres/006_market_data.sql`); idempotent `upsertMany` dedupe on `(commodity, market, source, observed_at)`; scheduler inert unless the flag is live AND a feed key is present | `FEWS_NET_API_KEY` and/or `NIMET_API_KEY` (optional `FEWS_NET_BASE_URL`/`NIMET_BASE_URL` overrides) | `drivers/market-data.drivers.ts`, `market-data-ingestion.service.ts` |
| LMS — Moodle | `LMS_DRIVER` | Fail-closed REST web-services client (`core_course_get_courses`, `core_enrol_get_enrolled_users`) | `MOODLE_BASE_URL` + `MOODLE_TOKEN` | `drivers/bridge.clients.ts` |
| Community — Discourse | `COMMUNITY_DRIVER` | Fail-closed API client (`latest.json`, topic read, post create; `DISCOURSE_API_USERNAME` default `system`) | `DISCOURSE_BASE_URL` + `DISCOURSE_API_KEY` | `drivers/bridge.clients.ts` |
| CMS — Directus | `CMS_DRIVER` | Fail-closed items client (`/items/{collection}`) | `DIRECTUS_BASE_URL` + `DIRECTUS_TOKEN` | `drivers/bridge.clients.ts` |

## Wave P5b lightweight-channel depth

| Driver | Flag | Live behaviour | Credentials (env) | Code |
| --- | --- | --- | --- | --- |
| USSD — Africa's Talking | `USSD_DRIVER=live\|sandbox` | `POST /api/v1/ussd/callback` accepts the form-encoded session turns (sessionId, phoneNumber, `*`-separated text) and answers `CON `/`END ` plain text ≤182 chars; pure step-based menu engine (register / market prices / opportunities / course enrolment / language stub) with a 3-minute session TTL, an expiry sweeper and idempotent replay on (sessionId, cumulative text). Endpoint is fail-closed: 404 until the flag AND credentials are set; production refuses to boot with the flag set and credentials missing | `AT_API_KEY` + `AT_USERNAME` (optional `USSD_SWEEP_INTERVAL_MS`) | `modules/ussd/` (`menu-engine.ts`, `ussd.service.ts`, `ussd.controller.ts`), `infra/postgres/008_ussd_channels.sql` |
| IVR — Africa's Talking Voice (wave P6a) | `IVR_DRIVER=live\|sandbox` | `POST /api/v1/ivr/callback` accepts the form-encoded call turns (sessionId, callerNumber, dtmfDigits, isActive) and answers Voice XML (`<Response><Say>`/`<GetDigits timeout="5">`/`<Enqueue/>`/`<Reject/>`); pure call-flow engine (commodity price check / crop advisory scoped to caller state else national / registration status with KYC tier + profile completeness / course enrolment status / 9 repeat / 0 agent escalation with callback mark) with a 10-minute call TTL, an expiry sweeper, 3-strike invalid re-prompt then polite END, and idempotent terminal replay on (sessionId, dtmf-history length). Endpoint is fail-closed: 404 until the flag AND credentials are set; production refuses to boot with the flag set and credentials missing. Prompts are plain-English text in a PROMPTS map — voice recording/TTS voice selection is provider-side | `AT_API_KEY` + `AT_USERNAME` (optional `IVR_SWEEP_INTERVAL_MS`) | `modules/ivr/` (`call-flow.ts`, `voice-xml.ts`, `ivr.service.ts`, `ivr.controller.ts`), `infra/postgres/011_ivr.sql` (`channels.ivr_calls`) |
| WhatsApp guided chat | `WHATSAPP_DRIVER` (wave P1) | Inbound 360dialog events drive multi-turn workflows (marketplace listing creation, crop-advisory requests, notification tap-to-confirm); workflow state lives in the shared KV store with a 24h TTL; consumes the wave P1 normalisation without driver changes | as per WhatsApp row above | `modules/notifications/whatsapp-workflows.ts`, `modules/notifications/inbound-conversations.service.ts` |
| Shared-device PIN swap | n/a (platform feature) | `POST /api/v1/auth/pin-profiles` (auth-guarded, max 5 profiles/device), `GET /api/v1/auth/pin-profiles/:deviceToken`, `POST /api/v1/auth/pin-sessions/switch` (4-digit PIN, salted hash, 5 attempts → 15-minute lockout, short-lived session issue) | none beyond platform auth | `modules/auth/pin-session.service.ts`, `modules/auth/pin-sessions.controller.ts`, `infra/postgres/008_ussd_channels.sql` (`channels.pin_profiles`) |

## Wave FABRIC middleware drivers

Fail-closed adapters for the prescribed middleware stack — one port per middleware, a deterministic stub as the DEFAULT (current behaviour everywhere), and a live driver selected by env that fails closed at boot (`ProviderConfigError`) when configured-but-unconfigured and at call time (`ProviderHttpError`/`ProviderRequestError` → 503, circuit breaker) — never a silent stub fallback. Full detail, runbooks and verified-vs-not per middleware: `docs/integration-fabric.md`. Compose profiles in `infra/docker-compose.yml` (none start by default). **No live middleware cluster was run in this wave; verification = unit tests + compose YAML validity (docker was unavailable, so no `docker compose config` run).**

| Driver | Flag | Live behaviour | Credentials (env) | Code |
| --- | --- | --- | --- | --- |
| Event bus — Kafka (Redpanda locally) | `EVENT_BUS_DRIVER=kafka` | kafkajs producer; domain events to `${KAFKA_TOPIC_PREFIX:-agric.domain}.{event.name}` after outbox append; circuit breaker | `KAFKA_BROKERS` (+ optional `KAFKA_TOPIC_PREFIX`) | `core/events/event-bus.driver.ts` |
| Workflow orchestrator — Temporal | `WORKFLOW_DRIVER=temporal` | `@temporalio/client` start on task queue `agric-platform`; credit loan disbursement workflow (score-check → ledger-record → notification) proves the port; worker bootstrap `src/workers/temporal.worker.ts` (not auto-started) | `TEMPORAL_ADDRESS` (+ optional `TEMPORAL_NAMESPACE`, `TEMPORAL_TASK_QUEUE`) | `common/orchestration/workflow-orchestrator.driver.ts`, `modules/finance/workflows/` |
| Search — OpenSearch | `SEARCH_DRIVER=opensearch` | Query path (`search`/`suggest`) via `@opensearch-project/opensearch`; trending/related delegate to in-process fallback; index mapping `infra/opensearch/agric-platform-index.json` | `OPENSEARCH_NODE` (+ optional `OPENSEARCH_INDEX`, `OPENSEARCH_USERNAME`/`OPENSEARCH_PASSWORD`, `OPENSEARCH_TLS_REJECT_UNAUTHORIZED`) | `drivers/opensearch.driver.ts` |
| Rate limiting — Redis throttler storage | n/a (`REDIS_URL` presence) | Redis `INCR`+`PEXPIRE` fixed-window storage shared across replicas; in-memory fallback without `REDIS_URL` (pre-existing wave P code) | `REDIS_URL` (already used elsewhere) | `common/rate-limit/redis-throttler.storage.ts` |
| Authorization — Permify | `AUTHORIZATION_DRIVER=permify` | REST `/v1/tenants/{t}/permissions/check` for `credit_loan`/`read` (proof scope: `GET /loans/:id` only); RolesGuard stays default | `PERMIFY_URL` (+ optional `PERMIFY_TENANT_ID`) | `common/auth/authorization-check.driver.ts` |
| Ledger backend — TigerBeetle | `LEDGER_DRIVER=tigerbeetle` | **LEGAL GATE: defaults OFF; proof-of-port only — NOT wired into money movement** (Postgres ledger stays system of record). `tigerbeetle-node` createTransfers, integer kobo, circuit breaker; admin diagnostics `GET /finance/ledger/backend-status` | `TIGERBEETLE_ADDRESSES` + `TIGERBEETLE_CLUSTER_ID` (both required) | `drivers/tigerbeetle.driver.ts` |
| Payments interop — Mojaloop | `MOJALOOP_DRIVER=simulator` | FSPIOP-shaped `/quotes` + `/transfers` against a Mojaloop simulator (adapter-contract proof — NO full switch deployment; helm-scale, out of scope) | `MOJALOOP_SIM_URL` | `drivers/mojaloop.driver.ts` |
| Identity — Keycloak realm `agricplatform` | n/a (realm import) | Realm with all 7 platform roles (`farmer`, `buyer`, `partner`, `chapter_lead`, `lender`, `enumerator`, `admin`); confidential `agric-api` client (placeholder secret); legacy `agric-platform` realm retained | `OIDC_ISSUER`/`OIDC_AUDIENCE`/`OIDC_CLIENT_SECRET` env pointing | `infra/keycloak/realm-agricplatform.json` |
| Edge — APISIX / open-appsec | n/a (compose profiles) | Declarative gateway routes (key-auth placeholder + limit-count mirroring the throttler); open-appsec agent + nginx attachment scaffold in front of web/api | `APPSEC_AGENT_TOKEN` (optional) | `infra/apisix/`, `infra/openappsec/` |
| Batch geo — Apache Sedona | n/a (compose profile) | One-shot Spark/Sedona job over MinIO lakehouse parquet (batch only, NOT in request paths) | S3A envs (local MinIO defaults) | `infra/sedona/` |

## GeoLibre evaluation (wave FABRIC)

[GeoLibre](https://github.com/opengeos/GeoLibre) was evaluated for the admin geo map and the Python sidecar. Verdict: **not integrated**. GeoLibre is a full end-user, cloud-native GIS *application* — a Tauri v2 desktop/mobile app plus a hosted web app (web.geolibre.app) built on React/MapLibre/DuckDB-WASM, with a PyPI package (`geolibre`) that wraps the same app for Jupyter notebooks. It is not an embeddable JS/TS geo library we could mount inside the Next.js admin map (embedding a whole GIS workbench inside an admin dashboard would be a poor fit with no supported embed contract), and the Python package is a notebook UI wrapper, not a server-side geoprocessing engine, so it does not fit the flood-ml sidecar either (which needs headless model inference, already covered by IBM Granite + Sentinel Hub). Its in-browser geoprocessing toolbox is genuinely interesting for a future analyst-facing tool, but that is a product decision, not an integration-fabric item. No code depends on GeoLibre; nothing was forced.

## Phase 1 integrations

| Integration | Phase | Purpose (PRD ref) | Adapter location | Local behavior | Production credentials | Verification method |
| --- | --- | --- | --- | --- | --- | --- |
| Keycloak | P1 | Central IdP; OIDC SSO, RBAC roles in JWT, phone-OTP via custom SPI; SSO source for Moodle/Discourse (12.2) | `infra/keycloak/` realm config; `apps/api` auth guards; `apps/web` OIDC client | Dev realm via `KEYCLOAK_URL=http://localhost:8080`, realm `agric-platform`; API accepts locally-issued or mocked JWTs in tests | Keycloak host (self-hosted or managed), realm/client secrets (`KEYCLOAK_CLIENT_SECRET`), OTP SPI wired to Termii | Code: guard/RBAC tests with signed test JWTs. Sandbox: login E2E against dockerised realm. Live: hosted realm + OTP smoke test — **cannot be live-verified without hosting + Termii credentials** |
| Moodle | P1 | LMS for Learning Academy: courses, enrolment, completion events webhooked back (12.2, M4) | `apps/api` learning module Moodle bridge; driver `LMS_DRIVER` | Stub returns fixture courses/enrolments; certificate issuance simulated from local completion events | `MOODLE_BASE_URL`, `MOODLE_TOKEN`, OIDC SSO config | Code: stub catalogue/enrolment tests. Sandbox: self-hosted Moodle docker + REST/webhook round-trip. Live: hosted Moodle with seed courses — external dependency |
| Discourse | P1 | Community forum: SSO login, group→category sync, moderation (12.2, M3) | `apps/api` community module Discourse bridge; driver `COMMUNITY_DRIVER` | Stub forum data; SSO handshake mocked; moderation queue local | `DISCOURSE_BASE_URL`, `DISCOURSE_API_KEY`, DiscourseConnect SSO secret | Code: stub post/moderation tests. Sandbox: dockerised Discourse SSO E2E. Live: hosted instance — external dependency |
| Directus | P1–P2 | Headless CMS for articles, resources, programme content, knowledge base (Ch. 10, M14) | `apps/api` content module CMS adapter (flag TBD) | Stub content fixtures from `packages/shared`; no live CMS required to build | `DIRECTUS_BASE_URL`, `DIRECTUS_TOKEN` | Code: content-render tests on fixtures. Sandbox: dockerised Directus sync test. Live: hosted CMS — external dependency (M14 is Phase 2 for full library) |
| Paystack | P1 (MVP per M18), escrow P2 | Payment collection, invoicing, escrow/split payments for marketplace deposits (M7, J2) | `apps/api` finance/payments adapter; driver `PAYMENT_DRIVER`; webhook endpoint with `WEBHOOK_SIGNING_SECRET` verification | Stub records payment intents and webhook payloads; escrow state machine exercised locally against `ORDER_STATUSES` | `PAYSTACK_SECRET_KEY` (live), webhook URL registration; escrow/split features enabled on account | Code: state machine + idempotent webhook replay tests. Sandbox: Paystack test keys full deposit→release flow. Live: live keys + Phase 2 gate QA of escrow — **cannot be live-verified without live keys and board-approved fee terms ⚖** |
| Flutterwave | P1 fallback / P2 | Secondary PSSP for collections and disbursements; Mojaloop substitute rail (M7, Appendix A.15) | Same payments adapter interface as Paystack (multi-driver) | Stub driver only; no live calls | `FLUTTERWAVE_SECRET_KEY` | Code: adapter-interface conformance test. Sandbox: Flutterwave test keys. Live: signed merchant agreement — external dependency |
| Termii | P1 | Primary SMS: OTP, transactional, bulk broadcast (12.2, M15) | `apps/api` notifications SMS adapter; driver `SMS_DRIVER` | Stub logs messages to delivery log; OTP flow uses fixed test code | `TERMII_API_KEY`, sender ID registration (DND-compliant route) | Code: stub send + delivery-log tests; OTP verify test with fixed code. Sandbox: Termii test credits. Live: real SMS to test handset — **cannot be live-verified without API key; blocks OTP-based auth at launch (blocker L4)** |
| Twilio | P1 fallback | SMS fallback if Termii deliverability degrades (M15) | Same SMS adapter interface | Stub driver; fallback routing simulated in tests | `TWILIO_ACCOUNT_SID`, `TWILIO_AUTH_TOKEN` | Code: failover-routing unit test. Live: Twilio account — external dependency (fallback only; not a launch blocker) |
| WhatsApp via 360dialog | P1 notifications out; structured replies P2 | Transactional + broadcast WhatsApp: opportunity alerts, event reminders, programme targeting (J1–J4) | `apps/api` notifications WhatsApp adapter; driver `WHATSAPP_DRIVER` | Stub logs template sends; inbound reply webhook mocked | `WHATSAPP_360DIALOG_API_KEY`, Meta Business verification, approved message templates | Code: template-render + webhook-signature tests. Sandbox: 360dialog sandbox number. Live: Meta-approved templates + funded account — external dependency |
| OneSignal | P1 | Web/mobile push notifications (12.2, M15) | `apps/api` notifications push adapter + web SDK; driver `PUSH_DRIVER` | Stub records push payloads; no device registration | `ONESIGNAL_APP_ID`, `ONESIGNAL_REST_API_KEY` | Code: payload/preference tests. Sandbox: OneSignal free tier to test browser. Live: production app ID — sandbox-verifiable, low launch risk |
| Mailgun / SendGrid | P1 | Transactional email: confirmations, digests (12.2, M15) | `apps/api` notifications email adapter (either provider); driver `EMAIL_DRIVER` | Stub renders templates to local outbox | `MAILGUN_API_KEY` or `SENDGRID_API_KEY`, verified sending domain (SPF/DKIM) | Code: template-render tests. Sandbox: provider sandbox domain. Live: verified domain — external dependency but low risk |
| Meilisearch (OpenSearch target) | P1 Meilisearch; OpenSearch at scale (Appendix A) | Full-text, typo-tolerant search across content, users, opportunities, listings, courses (M16) | `apps/api` search module adapter; driver `SEARCH_DRIVER`; `infra/` compose service | Stub in-memory search over fixtures, or local Meilisearch container in dev stack | `MEILISEARCH_HOST`, `MEILISEARCH_API_KEY` (self-hosted: none beyond network) | Code: stub search/facet tests. Sandbox: containerised Meilisearch indexing E2E — **fully verifiable without third-party credentials** |
| NiMet / OpenMeteo | P1 (OpenMeteo), NiMet agreement-dependent | Location-based weather alerts and seasonal guidance (12.2, M5) | `apps/api` advisory weather adapter; driver `WEATHER_DRIVER`; Open-Meteo live driver + state centroid table (wave P1) | Stub returns recorded weather payloads per LGA | OpenMeteo: no key required (open API). NiMet: institutional data-sharing agreement (no public API key) | Code: stub alert tests + mocked-fetch Open-Meteo driver tests. Sandbox/Live: OpenMeteo verifiable directly. NiMet — **external dependency (MoU); cannot be live-verified without the agreement** |
| FEWS NET | P1–P2 | Food-security / market intelligence snapshots for advisory (M5) | `apps/api` market-data ingestion scaffold (`MARKET_DATA_DRIVER`, wave P1) → `advisory.commodity_prices` | Stub fixture snapshots; scheduler inert without `FEWS_NET_API_KEY` | Public datasets (no credential); attribution required | Code: ingestion/parsing tests on published sample files — verifiable without credentials; cadence monitoring added in P2 |

## Phase 2 integrations

| Integration | Phase | Purpose (PRD ref) | Adapter location | Local behavior | Production credentials | Verification method |
| --- | --- | --- | --- | --- | --- | --- |
| FMARD price feeds | P2 data, P3 pipeline validation | Weekly national benchmark prices (rice, maize, cassava, sorghum, cowpea) for price intelligence (M5/M7) | `apps/api` advisory/market-price ingestion adapter (flag TBD) | Stub CSV/JSON fixtures of benchmark prices | Government data-sharing arrangement; no commercial API | Code: fixture ingestion tests. Live: validated against FMARD benchmarks (<5% variance, Phase 3 gate) — external dependency (MoU) |
| AFEX | P3 (prep in P2) | Daily commodity price indices; exchange trading partner (M7 route-to-market) | Integration ACL adapter `apps/api/src/integrations/afex/` (planned) | Stub price-index fixtures; no trading | AFEX partner API agreement ⚖ | Code: ACL translation tests on fixtures. Live: signed API agreement + trade controls — **cannot be live-verified without the agreement (Phase 3 gate ⚖)** |
| NCX | P3 (prep in P2) | Commodity exchange execution; lot aggregation for warehouse receipts (Appendix D) | Integration ACL adapter `apps/api/src/integrations/ncx/` (planned) | Stub order/confirm-trade fixtures; Temporal-style order lifecycle simulated locally | NCX integration API agreement ⚖; trade-control approvals | Code: ACL + settlement-validation tests on fixtures. Live: signed agreement — external dependency (Phase 3 gate ⚖) |
| Mojaloop | P2 | Payment interoperability/switching layer; open-loop rails via FSPIOP API (Appendix A.4) | Finance domain payment-switch adapter; `infra/mojaloop/` (deferred) | Not run locally in P1 — Paystack/Flutterwave PSSP used instead; Mojaloop hooks reserved in payment service interface so migration is a config change (A.15) | DFSP participation, FSPIOP-compliant switch access, CBN coordination ⚖ | Code: interface-conformance tests proving payment service is switch-agnostic. Live: **cannot be verified without DFSP onboarding — defer until >5,000 transactions/month trigger (A.15)** |
| TigerBeetle | P2 | High-integrity internal financial ledger: double-entry accounts and balances (Appendix A.5) | Finance ledger adapter behind storage-agnostic interface; `infra/tigerbeetle/` (deferred) | Phase 1 local behavior is the PostgreSQL double-entry ledger (accounts + transfers) — fully testable | Self-hosted; no third-party credentials; operational runbook required | Code: Postgres ledger balance/immutability tests now; TigerBeetle conformance tests against the same interface later. Introduce at >10,000 financial transactions/day or CBN audit trigger (A.15) |
| Kafka | P1 (target architecture) / P2 (adopt) | Central event backbone; domain event streaming, audit log transport (Appendix A.6) | Event-bus abstraction in `apps/api`; `infra/kafka/` (deferred) | Phase 1: internal bus (NestJS EventEmitter or Redis Pub/Sub) + Postgres outbox — keeps `{domain}.{entity}.{verb}` taxonomy testable without Kafka | Self-hosted or managed cluster; no external credentials | Code: outbox/event-contract tests now. Sandbox: containerised Kafka consumer contract tests when adopted |
| Temporal | P2 | Durable workflow orchestration: onboarding, payment settlement, loan application, impact reporting, exchange order lifecycle (Appendix A.6, D) | Workflow adapter wrapping saga steps; `infra/temporal/` (deferred) | Phase 1: simple Kafka/bus consumer + saga pattern in service code (A.15 simplification) | Self-hosted; no external credentials | Code: saga/compensation unit tests now. Sandbox: containerised Temporal workflow replay tests when adopted (trigger: saga inconsistency bugs at scale, A.15) |
| Dapr | P1 (target) / P2 (adopt) | Sidecar runtime: service invocation, pub-sub over Kafka, secrets, resiliency (Appendix A.6) | `infra/` deployment manifests; no app-code dependency (sidecar model) | Not present in Phase 1 local stack; services call each other directly | Self-hosted; no external credentials | Sandbox: sidecar injection + pub-sub smoke test in staging when adopted. Code: none required at baseline |
| Wazuh + OpenCTI (+ OpenSearch security analytics) | P2 | SOC: host/container security monitoring, HIDS, compliance scanning; threat-intel feeds pushing IoCs to edge blocking (Appendix A.8) | `infra/security/` agents and manager (deferred); consumes platform audit/auth logs | Not present in Phase 1 local stack; audit/auth logs written to Postgres for later ingestion | Self-hosted; external CTI feeds per feed licence | Sandbox: staging deployment ingesting test alerts. Live: SOC runbook + NDPR compliance scans — **operational dependency, Phase 2 gate for full SOC operations** |

## Phase 3 integrations

Wave P5a implements the federated model for these systems: identity resolution via phone/NIN with consent, inbound data tagged with source + consent timestamp, and fail-closed stubs by default. Code lives in `apps/api/src/modules/integrations/` — clients under `drivers/`, services + the `/integrations/federation` controller under `phase3/`, storage in `infra/postgres/007_phase3_integrations.sql`. Driver flags: `FARM_RECORDS_DRIVER`, `OFN_DRIVER`, `EXCHANGE_FEEDS_DRIVER`, `FIELD_DATA_DRIVER`, `LENDER_DRIVER`, `EXTENSION_FEED_DRIVER` (each `stub | sandbox | production | live`; a non-stub flag without the full credential set fails closed at boot). Webhook receivers (`/integrations/federation/webhooks/{farmos,litefarm,ofn,lender}`) use a shared-secret `x-integration-token` (`<SYSTEM>_WEBHOOK_TOKEN`); production traffic without a configured token is refused.

| Integration | Phase | Purpose (PRD ref) | Adapter location | Local behavior | Production credentials | Verification method |
| --- | --- | --- | --- | --- | --- | --- |
| farmOS / LiteFarm | P3 | Farm production records sync (crop plans, harvests, field maps) feeding credit profiles; outbound member verification push (Ch. 10, Appendix G; opt-in) | `drivers/farmos.clients.ts` (JSON:API + REST); `phase3/farm-records.service.ts` ACL adapter → `integrations.farm_records`; link/unlink endpoints | Stub: no clients; sync/push endpoints fail closed with a clear error; link/unlink + webhook ledger fully exercised | Farmer opt-in consent (`consentAt` on link) + `FARMOS_BASE_URL`/`FARMOS_API_KEY` and/or `LITEFARM_BASE_URL`/`LITEFARM_API_KEY`; webhook tokens | Code: ACL normalisation, replay-safe sync, webhook dedupe, consent-gate and fail-closed tests. Sandbox: public/demo farmOS instance. Live: per-farmer consent + partner instance — external dependency |
| Open Food Network (OFN) | P3 | Syndicate NYFN marketplace listings to OFN hubs; receive OFN order events mapped to marketplace order domain events with settlement status | `drivers/ofn.client.ts` (Spree-derived products API); `phase3/ofn-syndication.service.ts` | Stub: syndication endpoint fails closed; webhook ledger + settlement mapping testable | `OFN_BASE_URL`, `OFN_API_KEY`, `OFN_ENTERPRISE_ID`; `OFN_WEBHOOK_TOKEN` | Code: push payload (listing id as SKU), order-event mapping, replay and settlement-status tests. Sandbox: OFN staging hub. Live: hub agreement — external dependency |
| NCX / AFEX | P3 | Commodity exchange price feeds into advisory (extends M5 market data) | `drivers/exchange-feeds.drivers.ts` (NCX REST JSON; AFEX CSV pull over authenticated HTTPS, FTP-pull style); `phase3/exchange-feed-ingestion.service.ts` → `advisory.commodity_prices` (existing idempotent upsert) | Scheduler inert without flag + keys; manual admin pull endpoint available | `NCX_BASE_URL`/`NCX_API_KEY` and/or `AFEX_FEED_URL`/`AFEX_API_KEY`; exchange data agreements | Code: feed parsing, CSV tolerance, replay-safe ingestion, fail-closed tests. Live: exchange data licences ⚖ — external dependency |
| Kobo / ODK (KoboToolbox, ODK Central) | P3 | NGO/donor beneficiary import: bulk CSV/JSON staging, NIN/phone dedupe vs identity.users, admin confirm-before-merge (Appendix G.3) | `drivers/field-data.clients.ts`; `phase3/beneficiary-import.service.ts` → `integrations.import_batches/import_records` (SHA-256 identity hashes only); SFTP-pull scaffold fails closed until transport approval | Stub: no pull sources; admin batch upload + staging/confirm flow fully exercised in-memory | `KOBO_BASE_URL`/`KOBO_API_TOKEN`/`KOBO_ASSET_UID` and/or ODK Central vars; donor MIS agreements; SFTP per partner (scaffold) | Code: validation, within-batch + platform dedupe, consent-date tagging, merge-confirm tests. Live: partner data-sharing agreement ⚖ — external dependency |
| Input finance (lender bridge) | P3 | Anonymised consented credit-readiness push to lenders; inbound loan status/repayment events into the finance domain | `drivers/lender.client.ts`; `phase3/lender-integration.service.ts` (consent purpose `lender_data_sharing`, salted member-ref hash, uses finance CreditService via DI) | Stub: push fails closed; consent denial and webhook ledger testable | `LENDER_BASE_URL`, `LENDER_API_KEY`; `LENDER_WEBHOOK_TOKEN`; lender partnership agreement | Code: no-consent denial, revoked-consent denial, anonymisation, replay tests. Live: lender agreement ⚖ — external dependency |
| NAERLS / FMARD e-Extension | P3 | Read-only pest alerts / agronomy bulletins mapped into advisory items tagged with source + region | `drivers/extension-feeds.drivers.ts`; `phase3/extension-advisory.service.ts` → `advisory.items` (deterministic `ext-{source}-{id}` dedupe) | Scheduler inert without flag + keys; manual admin pull endpoint available | `NAERLS_BASE_URL`/`NAERLS_API_KEY` and/or `FMARD_BASE_URL`/`FMARD_API_KEY`; institutional MoUs | Code: bulletin normalisation, region tagging, replay dedupe, fail-closed tests. Live: MoU-gated — external dependency |
| OpenSearch (analytics/lakehouse role) | P3 (search role may arrive P2) | Log/metric/security analytics and large-scale search beyond Meilisearch (Appendix A) | `infra/opensearch/`; search adapter interface already abstracts engine | Meilisearch/stub covers local needs | Self-hosted; no third-party credentials | Sandbox: container reindex + query-parity tests against Meilisearch contract when adopted |
| APISIX (+ open-appsec WAF) | P1 target / P2–P3 adopt | Unified API gateway: TLS termination, JWT validation, rate limiting, routing; ML WAF/WAAP edge (Appendix A.7) | `infra/apisix/` gateway config (deferred); Phase 1 edge is Cloudflare + NestJS Throttler | Not in Phase 1 local stack; throttler tests cover rate-limit behaviour | Self-hosted; no third-party credentials | Code: throttler/header tests now. Sandbox: containerised APISIX route/JWT plugin tests in staging when adopted |

## Stage 18 polyglot sidecars (compose profiles, none start by default)

Three single-purpose sidecars ship as compose service fragments merged into `infra/docker-compose.yml`. Each is **stub-mode by default** and pairs with a fail-closed provider port in the API; the platform boots and passes all tests without them. **Verification = unit/contract tests + YAML validity; no live cluster was run.**

| Sidecar | Profile / port | Stack | Contract | Live mode |
| --- | --- | --- | --- | --- |
| `services/event-gw` | `event-gw` / 8090 | Go 1.22 stdlib-only | Webhook edge: HMAC-SHA256 verify (+/-300s skew, replay cache), fanout to API internal ingress, circuit breaker, JSONL dead-letter spool, `/metrics` | `EVENTGW_MODE=live` + per-provider `EVENTGW_SECRET_<NAME>`; unconfigured routes answer 503 |
| `services/crop-ml` | `crop-ml` / 8100 | Python 3.12 FastAPI | `POST /v1/crop/assess-plot` → NDVI/phenology/health-score with `basis` flag; consumed by credit geo-verification + VSLA carbon evidence | `IMAGERY_PROVIDER=live` + `SENTINEL_STATS_URL`/`_TOKEN`; 503 when configured-but-unreachable |
| `services/geo-compute` | `geo-compute` / 8200 | Rust axum + h3o | H3 index/compact, polygon metrics, geofence batch; validated against the repo's h3-js vectors | `GEOCOMPUTE_MODE=live` |

## Stage 18 innovation rails (T1–T3) — adapter ports and external gates

Ten innovation modules landed as append-style NestJS modules (migrations 027–037). Every external dependency is a port with a deterministic **stub default** and a **fail-closed** live driver (503 when configured-but-unreachable; production boot abort when a live flag lacks config). `basis` flags (`stub`/`live`/`estimate`/`unavailable`) are surfaced in API payloads and UI badges. **No live third-party integration was verified; each row's gate column states what activation requires.**

| Module (migration) | Port / secret | Stub default | Production gate |
| --- | --- | --- | --- |
| Voice agronomist (027) | ASR/TTS provider (`ASR_DRIVER`…) | Deterministic transcript stubs; agronomist escalation queue fully functional | ASR provider contract |
| EUDR traceability (029–030) | None external (hash-chained custody, DDS export) | n/a — fully functional on platform data | None for code; exporter onboarding is ops |
| Geo-verified credit (028) | crop-ml imagery (above) | Seeded NDVI stub; **shadow mode** scoring only | Imagery provider + lender policy sign-off |
| Parametric insurance (031) | Weather provider (`WEATHER_PROVIDER`…) | Labelled STUB rate card; payout execution simulated | Insurer MOU + payment rail ⚖ |
| Agent banking (032) | OTP driver (`OTP_DRIVER`…) + `AGENT_VOUCHER_SECRET` (prod-required) | Deterministic OTP; HMAC-signed offline vouchers; ledger-backed float | CBN agent-banking partner + OTP vendor ⚖ |
| Mechanization marketplace (033) | None external | n/a — fully functional | None for code |
| Warehouse receipts (034) | Certification feed + collateral registry (`WAREHOUSE_*_DRIVER`) + `WAREHOUSE_RECEIPT_SECRET` | Deterministic stubs; HMAC-signed e-WHR | Licensed warehouse network + collateral registry ⚖ |
| NIN input vouchers (035) | Identity adapter (`NIN_DRIVER`) + `NIN_HASH_SALT` (prod-required; plaintext NIN never stored) | Hash-derived deterministic verification | NIMC/licensed vendor + sponsor MOU ⚖ |
| Livestock passport (036) | Animal-ID authority (`ANIMAL_ID_AUTHORITY_MODE`) + `LIVESTOCK_PASSPORT_SECRET` (prod-required) | `STUB-NAIS-*` codes; passport aggregates existing health/permit/lien data end-to-end | State veterinary authority + tag registry |
| VSLA carbon MRV (037) | crop-ml NDVI (above, `CROP_ML_DRIVER`) | Versioned coefficient estimates `v1.2026.1`, labelled `basis:'estimate'` — never verification-grade | Carbon standard methodology + donor agreement ⚖ |

⚖ = requires qualified Nigerian legal/regulatory review before activation.

## Notes on live-verification limits

1. **Never live-verifiable from this repository alone:** Termii SMS delivery, WhatsApp template delivery, Paystack/Flutterwave live escrow and settlement, BVN/NIN checks (NIBSS/NIMC), NiMet feed, FMARD benchmarks, AFEX/NCX trading, Mojaloop switching, donor MIS imports. Each requires credentials, signed agreements, or regulatory review (⚖ items in `docs/security-compliance.md` §11).
2. **Fully verifiable without third-party credentials:** Meilisearch search slice, OpenMeteo weather path, FEWS NET public-data ingestion, Postgres double-entry ledger, event/outbox contracts, all stub-driver behaviour.
3. **Sandbox-verifiable now, live later:** Keycloak (self-host), Moodle, Discourse, Directus, Paystack test keys, OneSignal free tier, Mailgun/SendGrid sandbox domains, 360dialog sandbox. Sandbox evidence must be labelled `sandbox` and does not close launch blockers.
4. Every adapter must fail closed: unknown/missing credentials fall back to stub behaviour in non-production and to a loud, monitored configuration error in production — never a silent live call with placeholder keys.
